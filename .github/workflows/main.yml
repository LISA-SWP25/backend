name: CI Backend 

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

env:
  PYTHON_VERSION: '3.10'

jobs:
  # Мягкие проверки с автоисправлением
  gentle-check:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Set up Python ${{ env.PYTHON_VERSION }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ env.PYTHON_VERSION }}
        
    - name: Cache dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
          
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pytest black flake8
        
    # Автоисправление форматирования
    - name: Auto-format code with Black
      run: |
        echo "🔧 Автоматическое форматирование кода..."
        black .
        
    - name: Check if formatting changes were made
      id: format-check
      run: |
        if [ -n "$(git status --porcelain)" ]; then
          echo "changes=true" >> $GITHUB_OUTPUT
          echo "📝 Код был отформатирован автоматически"
        else
          echo "changes=false" >> $GITHUB_OUTPUT
          echo "✅ Код уже отформатирован правильно"
        fi
        
    # Коммит изменений форматирования (только для push, не для PR)
    - name: Commit formatting changes
      if: steps.format-check.outputs.changes == 'true' && github.event_name == 'push'
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add .
        git commit -m "🤖 Auto-format code with Black [skip ci]" || exit 0
        git push
        
    # Мягкий линтинг (предупреждения, не ошибки)
    - name: Code quality check (warnings only)
      run: |
        echo "🔍 Проверка качества кода..."
        echo "=== Flake8 проверки ==="
        flake8 . --count --statistics --exit-zero || true
        echo ""
        echo "=== Если есть замечания выше, рекомендуем их исправить ==="
        
    # Базовые тесты
    - name: Run basic tests
      run: |
        echo "🧪 Запуск базовых тестов..."
        pytest tests/test_api.py -v || {
          echo "❌ Некоторые тесты не прошли"
          echo "💡 Проверьте тесты локально: make test"
          exit 1
        }
        
    # Расширенные тесты (не блокирующие)
    - name: Run extended tests (non-blocking)
      run: |
        echo "🧪 Запуск расширенных тестов..."
        if [ -f "tests/test_api_extended.py" ]; then
          pytest tests/test_api_extended.py -v --tb=short || {
            echo "⚠️ Некоторые расширенные тесты не прошли (не критично)"
          }
        else
          echo "ℹ️ Расширенные тесты не найдены"
        fi
        
    - name: Success message
      run: |
        echo "🎉 Мягкие проверки завершены!"
        echo ""
        echo "📋 Что дальше:"
        echo "1. Проверьте предупреждения выше (если есть)"
        echo "2. Для полных проверок используйте: make quality"
        echo "3. Когда готовы - переключитесь на строгий CI/CD"

  # Опциональная сборка Docker (только при изменениях)
  build-if-changed:
    runs-on: ubuntu-latest
    if: contains(github.event.head_commit.message, '[build]') || github.ref == 'refs/heads/main'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Check if Dockerfile exists
      id: dockerfile-check
      run: |
        if [ -f "Dockerfile" ]; then
          echo "exists=true" >> $GITHUB_OUTPUT
        else
          echo "exists=false" >> $GITHUB_OUTPUT
        fi
        
    - name: Build Docker image (test only)
      if: steps.dockerfile-check.outputs.exists == 'true'
      run: |
        echo "🐳 Тестовая сборка Docker образа..."
        docker build -t lisa-backend-test . || {
          echo "❌ Ошибка сборки Docker образа"
          echo "💡 Проверьте Dockerfile и requirements.txt"
          exit 1
        }
        echo "✅ Docker образ собирается успешно!"
        
    - name: Docker not found
      if: steps.dockerfile-check.outputs.exists == 'false'
      run: |
        echo "ℹ️ Dockerfile не найден - пропускаем сборку"
